pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        // to allow osm road manager import to detect
        maven { url "https://jitpack.io"}
        mavenCentral()
    }
}
rootProject.name = "GoggleBird"
include ':app'
